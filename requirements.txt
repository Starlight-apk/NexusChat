# NexusChat - 工业级增强版依赖要求
# =====================================

# ========== 核心依赖（零依赖，仅使用 Python 标准库）==========
# 基础功能无需任何外部依赖即可运行

# ========== 可选增强功能 ==========

# YAML 配置支持 (可选，如未安装则使用 JSON)
PyYAML>=6.0

# 密码哈希增强 (可选，如未安装则使用 hashlib)
bcrypt>=4.0

# ========== 生产环境推荐依赖 ==========

# Redis 缓存支持（分布式缓存、会话共享）
redis>=4.5.0

# 性能优化：uvloop（Unix 系统下替代 asyncio 事件循环）
# uvloop>=0.17

# 监控指标导出（Prometheus）
# prometheus-client>=0.16.0

# 结构化日志
# python-json-logger>=2.0.0

# ========== 开发测试依赖 ==========

# 测试框架
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 代码质量
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0
